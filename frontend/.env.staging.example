# Vite auto-loads this for `vite build --mode staging`.
# Copy to .env.staging (gitignored) and fill in the real staging API origin.
VITE_API_BASE=https://staging-api.your-domain.example
# Must match the backend's API_KEYS for staging.
VITE_API_KEY=<from secret manager>
